# Assets

Add production assets here after design finalization:

- Geist font files, if bundling locally instead of using platform fonts
- app icon sources
- 곁핀 character assets
- store screenshot source media
